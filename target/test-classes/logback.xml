<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender: Always log to console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Sifting Appender: Logs routed dynamically based on testName from MDC -->
    <appender name="SIFT" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator class="ch.qos.logback.classic.sift.MDCBasedDiscriminator">
            <key>testName</key>
            <defaultValue>UnknownTest</defaultValue>
        </discriminator>

        <sift>
            <appender name="FILE-${testName}" class="ch.qos.logback.core.FileAppender">
                <!-- Log files will be created under test resources folder -->
                <file>Reports/Logs/${testName}.log</file>
                <append>true</append>
                <encoder>
                    <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
                    <charset>UTF-8</charset>
                </encoder>
            </appender>
        </sift>
    </appender>

    <!-- Set root logger level and attach appenders -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="SIFT" />
    </root>

</configuration>
